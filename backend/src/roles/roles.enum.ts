/**
 * @file roles.enum.ts
 * @brief Contains the different roles used in the application.
 */

/**
 * @enum {string} Role
 * @brief Enumeration for user roles.
 */
export enum Role {
  USER = "user",
  ADMIN = "admin",
}
