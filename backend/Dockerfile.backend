# Étape 1 : Utiliser une image Node.js
FROM node:22

# Étape 2 : Créer et déplacer dans le répertoire de l'application
WORKDIR /app

# Étape 3 : Copier les fichiers package.json et package-lock.json
COPY package*.json ./

# Étape 4 : Installer les dépendances
RUN npm install

# Étape 5 : Copier le reste des fichiers du backend
COPY . .

# Étape 6 : Exposer le port 3000
EXPOSE 3000

# Étape 7 : Démarrer le serveur
CMD ["npm", "run", "start:dev"]
